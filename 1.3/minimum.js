/**
 * Minimum Homework
 * Return the minimum values of 2 values
 */

/**
 * Return the minimum of 2 values
 * @param x
 * @param y
 * @returns {number}
 */
let min = function (x, y) {
    return Math.min(x, y);
}

console.log(min(0, 10));